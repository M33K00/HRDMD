<%- include('../../layouts/role1layout') -%>
<body>
  <%- include('../../layouts/nav') -%> <%- include('../../layouts/sidenavR1') %>
  <div class="page-body">
    <div class="page-name">
      <h2>Status Board</h2>
    </div>
    <%# Legend %>
    <div class="legend-container-r1">
      <div class="legend-item-r1">
        <span class="badge bg-danger">  </span><a>Rejected</a>
      </div>
      <div class="legend-item-r1">
        <span class="badge bg-warning">  </span><a> Entry Level</a>
      </div>
      <div class="legend-item-r1">
        <span class="badge bg-success">  </span><a> Individual Contributors</a>
        </div>
      </div>
    <%# Search bar %>
    <input
      class="form-control me-sm-2"
      type="text"
      id="accInput"
      placeholder="Search"
      style="margin-bottom: 20px"
      oninput="searchFunc()"
    />
    <div class="my-4">
      <div class="col-lg-12">
        <% if (message) { %>
          <div
            class="alert alert-dismissible alert-warning fade show"
            role="alert"
            id="autoDismissAlert"
          >
          <strong>Attention!</strong><%= message.message %><a
            href="#"
            class="alert-link"
            ></a>
        </div>
        <% } %>        
      </div>
          <% if (allRoleFiles != '') { %>
            <table id="table" class="table table-hover table-striped text-center">
              <thead>
                <tr class="table-primary">
                  <th>Date Submitted</th>
                  <th>File Name</th>
                  <th>Current Department</th>
                  <th></th>
              </thead>
              <tbody>
                <% allRoleFiles.forEach((row, index) => { %>
                  <tr>
                    <td><%= row.mtime %></td>
                    <td><%= row.name %></td>
                    <td><%= row.folder %></td>
                    <td><% if (row.folder === "Individual Contributors") { %>
                      <span class="badge bg-success table-span"> 
                        <% } else if (row.folder === "Entry Level") { %>
                          <span class="badge bg-warning table-span"> 
                          <% } else if (row.folder === "Rejected") { %>
                            <span class="badge bg-danger table-span"> 
                        <% } %></span></td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          <% } else { %>
            <h1 class="text-center text-secondary mt-5">No Users found in database</h1>
            <% } %>
        </div>
      </div>
    </div>
  </div>
<%- include('../../layouts/footer') -%>